name: CI Build and Dockerize

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

# Granting the GITHUB_TOKEN permission to create and push Docker images
permissions:
  contents: read
  packages: write

jobs:
  build-and-pack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      # 1. BUILD & TEST: Validates logic and generates the JAR
      - name: Build and Test
        run: mvn clean install

      # 2. NORMALIZE: Convert repository name to lowercase for Docker compliance
      - name: Lowercase Repository Name
        run: |
          echo "REPO_LOWER=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      # 3. DOCKERIZE: Package and Push
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Using the lowercase REPO_LOWER to avoid the "denied" error
          tags: |
            ghcr.io/${{ env.REPO_LOWER }}/fees-engine-core:latest
            ghcr.io/${{ env.REPO_LOWER }}/fees-engine-core:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
