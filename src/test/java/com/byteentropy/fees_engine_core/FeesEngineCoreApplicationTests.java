package com.byteentropy.fees_engine_core;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@SpringBootTest
@ActiveProfiles("test")
class FeesEngineCoreApplicationTests {

    @Test
    void contextLoads() {
        // Verifies that the Spring context, including Virtual Thread configs, starts correctly
    }

}